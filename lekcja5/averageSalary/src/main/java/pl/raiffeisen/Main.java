package pl.raiffeisen;

import java.math.BigDecimal;

public class Main {

    public static void main(String[] args) {
        String inputFile = "report.txt";
        AverageSalaryCalculator averageSalaryCalculator = new AverageSalaryCalculator(inputFile);
        BigDecimal averageSalary = averageSalaryCalculator
    }
}
